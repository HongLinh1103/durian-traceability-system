import { NextResponse } from "next/server";
import { getOwnedStore, requireRole } from "@/lib/store-marketplace";
import { prisma } from "@/lib/prisma";
export async function GET() { const s = await requireRole(["STORE_OWNER"]); if (!s) return NextResponse.json({ success: false }, { status: 403 }); const store = await getOwnedStore(s.user.id); if (!store) return NextResponse.json({ success: false }, { status: 404 }); const data = await prisma.order.findMany({ where: { storeId: store.id, deletedAt: null }, include: { items: true, farmer: { select: { fullName: true, phone: true } }, histories: { orderBy: { createdAt: "asc" } }, inventoryDocuments: { select: { code: true, type: true }, orderBy: { createdAt: "asc" } } }, orderBy: { createdAt: "desc" } }); return NextResponse.json({ success: true, data }); }
