import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { getOwnedStore, productUpdateSchema, requireRole } from "@/lib/store-marketplace";
export async function PATCH(request: Request, { params }: { params: { id: string } }) { const s = await requireRole(["STORE_OWNER"]); if (!s) return NextResponse.json({ success: false }, { status: 403 }); const store = await getOwnedStore(s.user.id); const item = store && await prisma.storeProduct.findFirst({ where: { id: params.id, storeId: store.id, deletedAt: null } }); if (!item) return NextResponse.json({ success: false }, { status: 404 }); const parsed = productUpdateSchema.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ success: false, message: parsed.error.issues[0]?.message }, { status: 400 }); return NextResponse.json({ success: true, data: await prisma.storeProduct.update({ where: { id: item.id }, data: parsed.data }) }); }
export async function DELETE(_request: Request, { params }: { params: { id: string } }) { const s = await requireRole(["STORE_OWNER"]); if (!s) return NextResponse.json({ success: false }, { status: 403 }); const store = await getOwnedStore(s.user.id); const item = store && await prisma.storeProduct.findFirst({ where: { id: params.id, storeId: store.id, deletedAt: null } }); if (!item) return NextResponse.json({ success: false }, { status: 404 }); await prisma.storeProduct.update({ where: { id: item.id }, data: { deletedAt: new Date(), status: "HIDDEN" } }); return NextResponse.json({ success: true }); }
