import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
export async function GET(request: Request) { const u = new URL(request.url); const type = u.searchParams.get("type"); const q = u.searchParams.get("q")?.trim(); const data = await prisma.storeProduct.findMany({ where: { deletedAt: null, status: "APPROVED", ...(type === "FERTILIZER" || type === "PESTICIDE" ? { type } : {}), store: { status: "APPROVED", deletedAt: null }, ...(q ? { OR: [{ name: { contains: q, mode: "insensitive" } }, { brand: { contains: q, mode: "insensitive" } }, { usagePurpose: { contains: q, mode: "insensitive" } }] } : {}) }, include: { store: { select: { id: true, name: true, address: true, phone: true, openingHours: true } } }, orderBy: { createdAt: "desc" } }); return NextResponse.json({ success: true, data }); }
